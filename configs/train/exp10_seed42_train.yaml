data: data/datasets/yolo/copy_paste/cp_t20_data_seed42/dataset.yaml
model: yolo11s.pt
epochs: 50
imgsz: 960
batch: 16
name: pill_exp10_seed42
device: "0"

# Augmentations
fliplr: 0.0
flipud: 0.0
degrees: 0.0
mosaic: 1.0
copy_paste: 0.0 # Using Custom (Offline) CP already in dataset

# Reproducibility
seed: 42
deterministic: True
